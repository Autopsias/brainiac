repos:
  - repo: https://github.com/semgrep/pre-commit
    rev: 'v1.168.0'
    hooks:
      - id: semgrep
        args: ['--config', 'p/python', '--config', 'p/secrets', '--error', '--skip-unknown-extensions']
