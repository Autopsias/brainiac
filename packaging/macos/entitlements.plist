<?xml version="1.0" encoding="UTF-8"?>
<!-- entitlements.plist — minimal hardened-runtime entitlements for the `brain`
     CLI. Hardened runtime is REQUIRED for notarization. We claim the LEAST:
     no JIT, no unsigned-memory, no dyld-env overrides, no debugger attach.
     `brain` does its work in-process (no child interpreters), so it needs none
     of the permissive entitlements. -->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <false/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <false/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <false/>
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <false/>
    <key>com.apple.security.cs.debugger</key>
    <false/>
</dict>
</plist>
