# PE version resource for the `brain` Windows binary (PyInstaller --version).
# Gives the PE a real identity so reputation engines don't read it as an
# anonymous dropper. Keep filevers/prodvers in lockstep with pyproject version
# (0.2.0 -> 0,2,0,0). CompanyName/ProductName are placeholders maintainer confirms
# at signing time — they MUST match the Azure Trusted Signing certificate subject.
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(0, 2, 0, 0),
    prodvers=(0, 2, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0),
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          "040904B0",
          [
            StringStruct("CompanyName", "Acme Energia"),
            StringStruct("FileDescription", "brain - local second-brain CLI"),
            StringStruct("FileVersion", "0.2.0.0"),
            StringStruct("InternalName", "brain"),
            StringStruct("LegalCopyright", "(c) Acme Energia. Internal use."),
            StringStruct("OriginalFilename", "brain.exe"),
            StringStruct("ProductName", "Profile A brain"),
            StringStruct("ProductVersion", "0.2.0.0"),
          ],
        )
      ]
    ),
    VarFileInfo([VarStruct("Translation", [0x0409, 1200])]),
  ],
)
